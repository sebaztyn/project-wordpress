version: '3'
services:
  db:
    image: 'postgres:latest'
    container_name: postgres_db
  myreact:
    build: .
    ports:
      - '3000:8080'
  myserver:
    build:
      context: server/
      dockerfile: server.Dockerfile
    ports:
      - '5000:5000'
